# Bell Data Intelligence — working agreement

Read this fully before touching anything. It is the operating contract for this repo, written by Claude with Val, 2026‑07‑10.

Val is the founder and sole operator. He is **not** a developer and does not read code. Explain in plain language, name the exact file/button/command for every step, and never assume he'll infer a missing step.

---

## ⏳ OPEN CHECKLIST — as of 2026‑07‑20 (calibration day + Bell Score / find‑anything‑search session)

**Ask him where he is in this list at the start of the session. Do not assume it's done.**
Full detail lives in memory: [[self-marketing-outreach]] and [[data-completeness-program]].

**🔴 A ROTATED TLS CERTIFICATE READ AS "PRODUCED NOTHING" FOR TWELVE NIGHTS — FIXED 2026‑08‑18:** on **2026‑08‑06** the Ministry of Finance rotated `*.mof.gov.qa`'s certificate and the new config sends **only the leaf** — the DigiCert intermediate is omitted. Browsers chase the missing link themselves (AIA); Node's `fetch` does not, so every plain‑fetch to Monaqasat died with `UNABLE_TO_VERIFY_LEAF_SIGNATURE` while the site opened fine for any human. The award scanner's first page failed, it `break`'d, and `recordJob` recorded a clean **`zero`** every night — and `alarmOnBrokenDuties` deliberately never alarms on "produced nothing", so nobody was told. **The tender scan kept working the whole time** because it renders through Crawl4AI/Playwright (real browsers do AIA). Fixed: `lib/certs/digicert-global-g2-tls-2020-ca1.pem` ships in the repo and `lib/tls_trust.js` adds it to the process CA store via `tls.setDefaultCACertificates` (Node 24), imported from **db.js** — the one module every Bell process loads. ⚠️ **Nothing is disabled**: the chain still verifies to Mozilla's roots; the missing link is merely supplied. ⚠️ `setGlobalDispatcher` from the npm `undici` package does **NOT** work — Node's built‑in fetch ignores it, and undici is not even a dependency here. ⚠️ The scanner now **throws** when its FIRST page is unreachable (a later page still stops gracefully), so a dead source is red, never silent. Caught up live: 800 reports read, 114 awards updated, 48 more winners linked.

**🏆 AWARD INTELLIGENCE — ALL FOUR SOURCES, 2026‑08‑17/18:** `composeAward` now speaks every source's own shape: **Monaqasat** full bid table (23,058 reports) · **Ashghal** `raw.bidders` with rank + ICV + prices (45 awarded, unread until now) · **Kahramaa** `raw.winners` — 332 award categories holding **503 winners**, of which the drawer showed only the first, so **171 winners and their individual amounts were invisible**; the block says "N **winners**" not "N bids" because Kahramaa publishes no bidder list and the label must not imply Bell knows who lost · **QatarEnergy deliberately gets no block** — it states one winner and its price and the drawer already prints both. ⚠️ Winner names resolve to companies through **`SQL_TIGHT`, the expression `companies_employer_tight_idx` already indexes** — my first version rolled its own normalization (dropping `&` where the shipped key expands it to ` and `): right answers, **312 ms and 32k buffer pages of seq scan per drawer open**. A test EXPLAINs it. **1,254 winners linked by stated CR** (`linkAwardWinnersByCr`, runs before the name pass in every ingest; ambiguity refuses); migration **120** indexes the base‑CR expression. **577 "Won tenders" discovery cards** (`relation='awarded'`): firms whose stated CR resolves to NO Bell company — AMEX Corporation won 82, Management Solution International 45 (QAR 57.6M). Approve → company created, **stated CR recorded as a `company_record` registration**, and the CR linker attaches the won tenders in the same click. Nightly leg `award_candidates`. **21 dedup pairs** queued from the same evidence (the award page names the winner twice — by name and by CR — and Bell held two rows: "Laffan Ref. Air Conditioning" ↔ "Laffan Refrigeration & AC", same CR).

**📋 JOB‑BOARD HONESTY — 2026‑08‑18:** `own_site` read **red every night for six nights** on a source that was working: `bySource[platform].error` was overwritten by EVERY failing board, so one 403 among **42 good reads of 69 boards** became the source's verdict (and `recordSourceOutcomes` discards `produced` when `error` is set). Now a per‑board failure is a `sample_error` + a count, and a source is only `error` when it is **structurally** unreadable (`read === 0`). ⚠️ Also: `boardsDue` gated on `last_ok_at IS NULL OR stale`, and **3,138 of 3,422** careers boards have NEVER succeeded (Qatar employers don't publish schema.org JobPosting) — so "never succeeded" meant "due every night" and the weekly cadence never applied. Migration **121** adds `last_attempt_at`, stamped by `recordSweep` on **both** paths (`updated_at` cannot serve: the harvester's board upserts touch it).

**🏆 AWARD INTELLIGENCE IS LIVE — 2026‑08‑17 (deep memory [[operation-data-trust]]):** the 23,058 Monaqasat award reports (winner + every LOSING bidder + CR numbers + proposal amounts + ICV) now have a product surface: tender drawer shows "Award result · N bids" (🏆 winner highlighted, bidders company‑linked via their STATED CRs, footer names the source), company profile shows "Government tenders — won N (QAR X) · bid without winning M" with "lost to <name>" on each loss (Mannai: 894 wins QAR 5.82B, 100 losses — verified live), Bella has `get_company_awards` (72 tools). Migration **119** GINs `raw->'award_report'->'bids'` for the lost‑bids containment probe; `tests/award_intel.test.mjs` EXPLAINs the SHIPPED query so index and query can never drift (the migration‑113 lesson). ⚠️ **Winner links are name‑matched, CRs are stronger:** tender 34778's winner column points at "East West Trading International" (id 188380, NO registrations) while the bid's stated CR resolves to "East & West Trading International Est." (id 170443, CR 24795 MOCI+QCCI) — same firm, two rows, exactly Val's duplicate complaint. Reconciliation + dedup‑candidate feed is task #108, measure running. **Also fixed same day:** 2,069 tender titles began with Monaqasat's own STATUS BANNER ("Tender is violation due to delay" / Arabic twin) stored as title text — exactly four banner strings exist across 27k titles (measured); the scraper now moves them to `raw.status_banner` (`splitStatusBanner`) and 1,787 stored titles were repaired (282 kept — nothing but "0" behind the banner).

**📧 PER‑TENANT SMTP IS LIVE — 2026‑08‑18 (Val: "lets build it all, with bounce and open emails"):** a customer's CRM mail can now leave through **their own mail server** (Settings → Sending domain → "Use my own mail server"). Migration **122**. The transport branch lives in **ONE place — the `sendEmail` chokepoint** — so suppression, `contact_guard`, the daily limit and the `email_log` ledger all still apply; no tenant SMTP, an outreach send, or local‑admin with no tenant → Resend as before. ⚠️ **The hard part was never sending, it was knowing what happened next:** Resend reports opens/bounces by webhook, a customer's server reports nothing. So **bounces** are read from the mailbox the tenant nominates (`crm/smtp_bounce_poller.js`, gate `BDI_SMTP_BOUNCE_POLLER=1` — ONE service, admin shares prod's DB) and reach the same end state as the webhook (status `bounced` + global suppression + contact downgrade); Bell **never marks their mail `\Seen`** and never deletes — it tracks `imap_last_uid` instead. A **4.x.x temporary failure suppresses nothing** (a full mailbox is not a dead address), and the suppressed address is **the one on Bell's own row**, never one parsed from the inbound message (anyone who can post mail to that mailbox could otherwise suppress anybody). **Opens** use a per‑message pixel (`/t/o/:token.gif`, always returns the image) recorded as "the image was fetched" — never upgraded to "read" (image blocking hides real reads; Gmail/Apple proxies fetch before a human looks). ⚠️ **Credentials are write‑only** — AES‑256‑GCM via `lib/secrets.js` (the `BDI_KEY_PII` key), and **`connectCustomDomain`/`verifyCustomDomain`/`updateIdentity` all did `RETURNING *` straight into HTTP responses**, so migration 122 would have shipped a tenant's encrypted mail password to the browser; all three now go through the one `publicIdentity()` allow‑list. ⚠️ **Saving settings RESETS verification** and the send path refuses anything but `status='verified'`. A refused login is reported **in the mail server's own words**. **Proven end‑to‑end against a real SMTP server** (Mailtrap sandbox — nothing reached a real person): test signed in · mail delivered · the delivered HTML carried the pixel with the matching token · the fetch flipped the row to `opened`. 470 tests pass.

**🔓 QCCI IS UNBLOCKED — REAL CHROME + A PERSISTENT PROFILE, 2026‑08‑18:** measured on qatarcid.com from Bell's own network, in one sitting: **Crawl4AI headless challenged 5/5 · Crawl4AI WITH the new stealth flags challenged 5/5 · an automated WINDOW using Playwright's bundled Chromium challenged 3/3 · a genuine browser at the same IP loaded the page in full (82 KB)**. So the block was never the address and never the window — it is the automation fingerprint of a throwaway browser. `renderPage(url, { headed: true })` now drives the **real Chrome binary through a persistent profile** (`.bell-headed-profile/`, gitignored) so a satisfied challenge is not met again as a brand‑new visitor. **Proven end‑to‑end: 3/3 listings read, parsed and INGESTED** where every previous attempt scored zero — that unfreezes **41,951 QCCI listings** (stale since 9 June) for free. ⚠️ Headed is allowed **by default only on win32** (the ROG's tasks already run Interactive logon, so a window disturbs nobody); everywhere else it needs `BDI_ALLOW_HEADED=1`, which `Read QCCI Directory Now.command` now sets. ⚠️ The same lever is the obvious next move for **MOCI**, whose WAF also 503s only headless.

**🔴 THE MERGE GUARD COMPARED A QFC LICENCE AGAINST A CR — fixed 2026‑08‑18:** `companies.primary_registration_no` is not "the company's CR" — it can hold a QFC **licence**, or an arbitrary one of several registrations. The guard compared that ONE scalar per side and refused any merge where they differed. Measured across the **5,831 live cross‑body pairs: it refused 110, and in 110 of 110 BOTH companies state the shared base CR** in `company_registrations` (#3973's "primary" is QFC licence 02087 while the same company states commercial registration 00040727). `registrationBases()` now returns the SET of stated commercial registrations and the guard refuses only when the two sets are **disjoint**; the primary field is a fallback for thin records, never folded in beside real CRs (a licence must not manufacture an overlap with a stranger's CR). ⚠️ **The automatic merger deliberately does NOT follow the relaxed guard all the way**: where the two records disagree about their primary number the pair goes to the **Dedup Queue** with its evidence instead of merging — "Tadmur Holding"/"Tadmur Trading" state each other's CRs and may still be two legal entities, the iHorizons shape. Also: `failedBy` now rides the result into `job_runs` (`summarize()` keeps nested numbers) and the merger passes a REAL logger — "failed: 12" previously cost a full investigation because the reason lived only in a Windows task log.

**🕵️ CRAWL4AI SETTLE/STEALTH — 2026‑08‑17:** `crawl4aiRender` forwarded ONLY the timeout — a caller's `settleMs` reached just the Playwright fallback, so on the ROG (where Crawl4AI serves every render) the QCCI probe captured Cloudflare's interstitial before its challenge JS could resolve. Now settle/wait_selector/js_code/stealth all pass through; the server takes a per‑crawl `stealth` flag (magic + simulate_user + override_navigator, `enable_stealth` on the browser, all version‑guarded) and the QCCI probe requests it. ⚠️ The ROG's Crawl4AI service runs the OLD server code until that service restarts — there is no supervisor I could verify, so no blind kill; if the nightly QCCI probe still reads red after a ROG reboot, that is the real signal. **guess_recheck is a nightly duty now:** 13,020 single‑owner guessed websites face `gateDecision` ~400/night — page states the name → `method='guess_confirmed'`; parked/dead → withdrawn only on the SECOND night (tombstones + resync; one bad night is never evidence); unproven → stamped, left standing. On the Scheduled‑duties card.

**🏗 MOCI PROVING RUN — BLOCKED 2026‑08‑17:** businessmap.moci.gov.qa serves its 503 shell to HEADLESS Chromium persistently (two runs, ~24 rounds, direct + parent‑site nav both land on a 320‑char 503 page after 90s waits). The scraper's own remediation stands: `HEADLESS=false` (visible browser) usually passes. **Do NOT wire the nightly staleness leg until a headful run proves the parser** — a leg that reds every night is noise. Options for Val: a watched headful run on the Mac, or add MOCI to the ROG's Saturday registry rotation with a visible browser (its tasks run Interactive logon, so a window there disturbs nobody).

**🔴 SEVEN PRIVATE USER‑CONTRIBUTED PEOPLE WERE LIVE ON PRODUCTION — removed 2026‑08‑09:** the 7 prod‑only `people` (ids 2000040084‑90) were NOT research output, which is what I called them twice before reading the rows. They are `created_via = user_contributed` with `private = true` — names "Jack", "Jack Jack", "Ashot Bleyan 1‑5", i.e. records made through the customer **"+ New"** flow on app.bell.qa. `CONTRIB_EXCLUDE.people` says those must **NEVER** reach prod until the lawyer‑gated promotion clears the private flag. They were there not because a push sent them — the syncWhere blocks that — but because they were **BORN there**: the contribution flow creates them on prod, drawing ids from prod's own `people_id_seq`, which also sits in the 2e9 band. The mirror had no way to remove a prod‑born contributed row until `reconcileDisqualified()` (added the same day) caught them. **They are now off prod and on the engine box, where the review queue lives** — the correct end state. ⚠️ **An id alone cannot tell you who created a row.** The band holds THREE writers: local people (72,206), prod‑born contributed people, and prod research entities. `find_prod_orphans.js` now REPORTS a high‑band prod‑only row for a look instead of either deleting it or waving it through — both of which it did, in successive versions, before anyone read the rows.

**📧 THREE WAYS TO SEND AN EMAIL, NONE OF THEM COUNTING — FIXED 2026‑08‑10:** Val's 🔴 item from 2026‑08‑06 ("she does not consider that those companies have been reached out to already… make sure the user is aware, and PREVENT sending too many emails without the user's knowledge"). Three paths could mail a Qatar company — the single send, the bulk send, and a sequence step — and **not one read what had already been sent, nor `email_suppressions`**: an address that hard‑bounced and was globally suppressed for the outreach machine could still be mailed from the CRM, repeatedly. (`lib/suppression.js` says "the send path calls filterSuppressed() before every send" — true of the outreach machine, of nothing else.) **`server/crm/contact_guard.js` is now the one chokepoint all three pass through:** `address_suppressed` (bounced/complained/unsubscribed — **never** overridable) · `duplicate_email` (same subject, same address, inside 24 h — not a decision to send again, it is the same email twice) · `recently_contacted` (2+ accepted sends in 14 days — **overridable, and meant to be**: it exists so a person is told, not to forbid). ⚠️ Only a **provider‑accepted** send counts as contact — a row still `queued`, or one that `failed`, must not block the retry. ⚠️ **Sequence steps are pre‑acknowledged for the ceiling ONLY** (the user wrote the steps and enrolled the record — that IS acknowledging a cadence); a suppressed address stops the enrolment rather than bouncing through its schedule. **Bella:** new `preflight` hook runs BEFORE the approval card — a refusal raises no card at all (asking permission to make a mistake is not a question), and any card that IS raised now carries "2 emails already sent to X, most recently 3 Aug". ⚠️ Its handler takes `acknowledgePriorContact` first, so the button MUST be wired `onClick=${() => sendEmail()}` — a bare reference passes the click event, always truthy, and would defeat the guard on the very first click. **Also fixed:** the bulk send and sequence step still resolved the recipient from the legacy `companies.email` column while the single send read `company_contacts` first — the same record was mailable at **two different addresses depending on which button was pressed**. One resolver now (`crm/recipient.js`); the legacy fallback is KEPT because it is real — of the 227 live companies whose address exists only in that column, **203 are stated verbatim in that company's own registry payload** (QCCI 142 · Tasmu 67 · MOCI 51 · QFZ 22; none match the company's own website domain because they are QFZ/QFC subsidiaries whose registered contact genuinely is the parent's mailbox). Migration **116** indexes `(tenant_id, lower(btrim(to_email)))`, proven used by EXPLAIN. 27 new tests against real Postgres; all four refusal paths exercised through the live HTTP route with nothing written to `crm_emails`.

**📮 AN ADDRESS ON FILE IS NOT NECESSARILY AN ADDRESS — 2026‑08‑10:** measured on the engine box, **220 of 19,449 `company_contacts` emails and 236 of 12,343 legacy `companies.email` values are strings a mail provider rejects outright** — real harvested data, not corruption: `"LILAC.FASHION @HOTMAIL ,COM"` · `"mmaa@ mmaa.gov.qa"` · `"amusaid@yahoo"` · `"albateel@qatar.net .qa"` · `"info@buildimgsga0com"`. Nothing checked them: the ONLY validity test on the entire send path lived at Resend, which answered with a 422 that `routes/crm.js` turned into a blank *"Could not send the email."* Now `crm/recipient.js` classifies, and the route answers **`bad_address` naming the value verbatim** (the UI drops it into the To box to be corrected). ⚠️ **Bell must NOT repair these** — `"LILAC.FASHION @HOTMAIL ,COM"` obviously *means* `lilac.fashion@hotmail.com`, and writing that is a guess about a real person's mailbox. Rule 2.1 does not bend because a guess feels safe. Also fixed: **`includes('@')` is not a validity check** and was used in 4 places — worst at `/recipients`, which BUILT the cc tick‑box list, so Bell was *offering* Val addresses it knew could not be sent; and `getFromAddress()` did `String(jsonb)`, which turns a jsonb `null` into the truthy string `"null"` so the `|| DEFAULT_FROM` fallback could never fire (latent — nothing writes that key today).

**💼 "HIRING IN QATAR" — a missing company is now a lead, 2026‑08‑10:** Val, looking at the Jobs tab: *"many jobs are not having company, which does not make sense."* Two causes, one fixable. **34 Oracle requisitions state NO employer** (that platform publishes none — verified 86/86), so nothing can be matched and they stay unattached, correctly. The other **117 classifieds vacancies DO name their employer** — Bell just has no company by that name. Those are **51 real Qatar firms, provably trading and provably hiring, missing from the database**: Fornax Global (10 vacancies), C2HR Global (10), Auralon Engineering (9), Nasser Al Jaber Group (9), Trilogistics WLL (8), Al Majed Group (8)… New **"Hiring in Qatar"** tab in Discovery Review (50 queued; 1 rejected as too vague by the same specificity gate the matcher uses). Each card carries its evidence — vacancy count, the job titles, the location. **Approve → the company is created AND its vacancies attach automatically** (proven live: Fornax Global → company #197453, 10 jobs linked in the same request). Reuses `spark_discoveries` with `relation='hiring'` so it inherits promote/ignore/dedup; the Spark tabs and counts exclude it. Queued nightly after the job sweep. ⚠️ Nothing is created without Val's click — an advert is a claim about who is hiring, not proof a company exists under exactly that name.

**💰 RAILWAY COST — MEASURED 2026‑08‑10, and one figure here was WRONG:** engine box DB **4,115 MB**. ⚠️ **"`od_records` is LOCAL‑ONLY — it costs Railway nothing" is FALSE.** It is not MIRRORED, which is what `sync/tables.js` says and what I read it as — but `startOpenDataScheduler()` was the ONE scheduler in `server.js` with **no mode gate and no env gate**, so **every deployment BUILT ITS OWN COPY** from the open‑data source: 2,479 MB across 4,027,430 rows (1,713 MB heap + 511 MB indexes + a 371 MB GIN), on production AND on staging AND a duplicate worker on admin. *Not mirrored is not the same as not there.* Now gated: `MODE !== 'admin' && BDI_OPEN_DATA_SYNC !== '0'` — production keeps it (Qatar Market Pulse derives from it and `/api/open-stats` is customer‑facing), admin stops double‑syncing the same DB, and **staging is turned off with `BDI_OPEN_DATA_SYNC=0`**. ⚠️ `idx_od_records_data` (GIN on `data`, **371 MB, 0 scans since the ROG flip**) is a real candidate but NOT dropped: the operator `data ? $2` in `chart_picker.js` IS GIN‑indexable, so it is not dead code — the planner simply never picks it because every query scopes by `dataset_id_fk` first (measured: the plan does not use it, 3.4 s either way). Decide it with a measurement, not a reflex. **New: `GET /api/stats/storage`** (platform‑admin) reports database size, the 25 biggest tables split heap/indexes, and big rarely‑scanned indexes — so this conversation is never had from an estimate again. The fat is in indexes: `companies` was 792 MB **of which 464 MB was indexes**. `idx_companies_extra_fields_gin` = **114 MB, used 0 times** in three weeks (its only query shape lives in engine‑box registry ingests, which RAN and still got a seq scan; production never queries it) → dropped in migration **115**, `companies` now 679 MB / 350 MB. ⚠️ **KEEP `idx_companies_search_blob_trgm` — MEASURED 2026‑08‑10, and dropping it would make RAM WORSE, not better.** It is 205 MB on prod (293 MB on the Mac copy) and used only 9× in three weeks, so it looked like the obvious next cut. Benchmarked on the disposable Mac copy against the real query shape from `routes/companies.js`, six phrases: **with it 436 ms total / 73 ms avg; without it 2,329 ms / 388 ms — 5.3× slower.** The number that settles it is not the time but the **buffer traffic**: one phrase search touches **117 MB** of pages with the index and **1,729 MB** without it (the heap is 362 MB, but `search_blob` is TOASTed, so a seq scan detoasts its way through far more). Val's complaint is Postgres holding ~7 GB — dropping this index would push **15× more data through shared_buffers on every phrase search**. It was measured by disabling the index's plan (`enable_bitmapscan/indexscan off`), never by dropping it; the ROG was not touched. ⚠️ **NOT stripped from the mirror:** `company_sources.raw_payload` (101 MB) — it IS displayed in the admin drawer (`CompanyDetail.js:1333`), so removing it costs a capability, not waste. **The likely bigger lever is STAGING** (a second Postgres at ~5 GB continuously); Val 2026‑08‑10: staging needs real data *"maybe not for now but for later"*.

**🔴 THE RESEARCH ID BAND OVERLAPPED LOCAL'S OWN PEOPLE SEQUENCE — migration 114, 2026‑08‑09:** migration 017 reserved ids ≥ 2,000,000,000 for entities PRODUCTION creates (research started on bell.qa runs on Railway and writes straight to prod), on the premise that "local ids (~10^5) will never reach it". **That premise is false.** Measured on the engine box: `people` 72,398 rows, **72,206 of them in the band**, `people_id_seq` = **2,000,073,304**; `companies` is fine (0 in band, seq 197,420). Prod's `research_entity_id_seq` had reached ~2,000,040,090, so **its next value named an id the engine box had already given to a different person** — one research job from silently overwriting a real PDPPL record with another. Migration **114** moves the band floor to 3,000,000,000 (`GREATEST`, idempotent, both deployments). ⚠️ Code that TESTS the band still reads ≥2e9 (`sync/pull.js` HIGH_BASE, `find_prod_orphans.js`) and must stay that way — the entities already issued between 2.0e9 and 2.1e9 are real and must keep matching. ⚠️ **Do NOT add a "pull everything in the high band" clause to `collectResearchPull`** — I tried it to recover 7 stranded people and it selected essentially the whole `people` table. It ran twice before the revert reached Railway and rewrote **all 72,206** local people with prod's copy. **Verified afterwards: no data lost** — emails 459 before and after, names/headlines/LinkedIn/72,327 company links all intact, because prod's copy IS what local last pushed. The only cost was a `updated_at` churn (BEFORE UPDATE touch trigger) that re‑queued 72k rows for the next push, cleared immediately. ⚠️ **A revert is not live until RAILWAY has redeployed** — a queued job ran against the old prod code minutes after the git push. Wait for the deploy, or verify by calling `/api/sync/research-pull` with `since` = epoch and checking the row count, before assuming a fix is in effect. The ledger is the only correct signal.

**🔴 A LINK FAILURE ERASED A PERSON'S LEDGER ROW — fixed 2026‑08‑09:** `processPerson` wrote the `research_derived_entities` row LAST (insert → link → audit) inside one try whose catch records `skipped` with a **null id**. A failure in the linking step therefore let the savepoint RELEASE and commit a real person with no ledger entry — and the ledger is how `collectResearchPull` finds prod‑created people, so those 7 were unreachable even from the epoch. Now the creation is recorded immediately after the insert and linking has its own try/catch. *A person without an employment link is incomplete; a person without a ledger row is lost.* Same fix on the enrich path.

**🔴 PRODUCTION IS SERVING 10,325 ROWS BELL DELETED — FOUND 2026‑08‑09, AWAITING VAL'S CLICK:** prod is a strict id‑mirror with **zero delete triggers**, so a hard delete only lands via a `sync_deletions` tombstone. Every bulk DELETE that forgot one left the row alive on app.bell.qa forever while every count on the engine box looked healthy. Comparing counts after a push showed people 72,334 on prod vs 72,327 local; a full id‑level audit of all 29 mirrored tables then found: **person_companies 10,026 · company_tech 240 · company_contacts 41 · people 7 · knowledge_sources 6 · person_contacts 5**. Sampled ids verified genuinely absent locally, and `sync_deletions` was empty (so nothing was merely pending). ⚠️ **company_contacts is the one that bites**: those are contacts Bell deleted *because they belonged to a different company* — the [[legacy-contact-column]] incident — still live and revealable to customers. New: `GET /api/sync/ids` (sync‑token, ids only — `people` is PDPPL‑sensitive) + `scripts/find_prod_orphans.js`. **Val's clicks: Find Production Orphans.command** (read‑only) then **Apply Production Orphan Cleanup.command** (types `yes`, writes tombstones, pushes). ⚠️ A FILTERED mirror (`company_relationships` has a syncWhere) must have that filter applied to the local side or the tool cries wolf about 216,538 rows that are deliberately absent.

**🔴 THE NIGHTLY DIED AND NOTHING SAID SO — FIXED 2026‑08‑09:** on 2026‑08‑09 the ROG's nightly started 00:30 Qatar, self‑updated, and wrote NOTHING for the next 16 hours. Eight duties live in its `finally` block (tender scan · award reports · close‑expired · QSE · job boards · registry merge · chain links · weekly data check · Bell Score heal) and none was reached — the time budget is only consulted BETWEEN rounds, so one wedged round eats the night. In `job_runs`, which records only COMPLETIONS, that looked like silence, i.e. identical to "never scheduled". Shipped: each harvest round is raced against a 90‑min ceiling (losing costs one chunk, not the night) · `openJob()` writes a **'running'** row at the START so a hang reads as **"started but never finished"** · `qse_scan`/`job_sweep`/`registry_merge` added to SCHEDULED_KINDS (they could have stopped without ever being flagged) · the always‑on engine checks duty health **hourly** and emails once per duty per 20h (it is the one process guaranteed to be running when the nightly is not; Sunday's report would have meant six days of silence). Claude re‑ran the browser‑free duties by hand that day (QSE 428 refreshed, scores healed); the tender scan + award reports waited for the next night. **Val sees it at:** local Portal → **Local Engines** → new **"Scheduled duties"** card.

**🔴 A DEAD SOURCE HID INSIDE A HEALTHY TOTAL — FIXED 2026‑08‑09:** Kahramaa returned HTTP 401 for **fourteen consecutive nights** while the tender scan recorded "ok" every time — three healthy portals produced plenty, the scan reported ONE aggregate, and `summarize()` threw the per‑source breakdown away. Now `recordSourceOutcomes()` writes **one `job_runs` row per source** (status `zero` = read fine, produced nothing, distinct from ok/error), `silentSources()` names any source with no good run in N days, the weekly data check has a section for it, and a parent job whose total looks fine while a source is dead reports **"a source is failing"**. ⚠️ The pre‑existing "gone quiet" section reads `company_sources`, which tender scans never touch — it could not have caught this.

**💼 JOBS — ALL FOUR READERS WIRED 2026‑08‑09 (Val's 2026‑08‑07 instruction):** three readers were built and tested a session ago and only ONE was wired in. Now live: the **national career portal** (249 postings, read at the 5 s crawl delay its own robots.txt states, ~21 min) · a **classifieds board** carrying many employers (233 live vacancies, every one naming its employer) · **every company's OWN careers page** (255 recorded by the harvester, read by nobody until now — takes **schema.org JobPosting ONLY**, data the site publishes for search engines; scraping the visible page is how a "Life at X" testimonial becomes a vacancy) · the 5 Oracle tenants. **Attribution is now per JOB, not per board** (`server/jobs/attribute.js`): the posting must NAME its employer and that name must land on exactly one active company (word‑preserving key — `normalizeName` strips holding/company/trading and already merged two real firms once; ambiguity refuses rather than picks). Live: **111 of 233** classifieds vacancies tie to a Bell company via 19 employer names; Oracle states no employer on 86/86 requisitions so its jobs correctly stay unattached. Migration **112** indexes the three name keys (a lookup was 2.4 s, now 11–45 ms). ⚠️ **Val's instruction honoured:** `source`/`source_url`/`board_key`/`external_id`/`raw_payload` are stripped from the jobs API for everyone but platform_admin + local engine, and Bella's job fields are an allow‑list that never contained them. A vacancy Bell cannot tie to a company shows `employer_stated` marked **"as advertised"**. ⚠️ **Do NOT lower the guards:** a partial board read must FAIL (my first cap of 120 on a 249‑job portal would have closed 129 LIVE vacancies as "withdrawn"), and an incomplete classifieds crawl closes nothing.

**🔴 A REACT FLIGHT POINTER IS NOT TEXT — caught in the browser, fixed 2026‑08‑09:** the classifieds list page is a React Server Components payload, where a string beginning with `$` is a POINTER to another chunk ("$2d" = "see chunk 2d") and a real leading `$` is escaped by doubling. Its four long‑text fields (`job_description`, `job_description_ar`, `company_about`, `company_about_ar`) arrive as pointers whose chunks the site streams LATER — **measured across 60 listings on 5 pages: not one is present in the delivered HTML.** The first reader passed them straight through, so Bell stored **158 of 233 job descriptions as the literal strings "$2d", "$31", "$34"** and the Portal showed one to a customer as the job description. Spotted by looking at the actual screen, not the tests. Fixed: `unflight()` in the qatarliving reader is now the choke point for BOTH `statedString` and `qlHtmlToText` — a pointer is ABSENT (null, per Rule 2.1), and `$$foo` correctly unescapes to `$foo`. The 158 stored values were cleared. ⚠️ Note `upsertJobs` uses `COALESCE(EXCLUDED.description, jobs.description)`, so fixing the parser alone would NOT have cleared them — a bad value already stored has to be removed explicitly. A sweep of all **647 text columns** in the database found no other pointer‑shaped values (11 in `company_financials.value_text` are real currency strings — "$85B", "$124B" — from Firecrawl, unrelated).

**🔴 CLOSURE FIRED ON THE FIRST MISS, NOT THE SECOND — caught by adversarial review, fixed 2026‑08‑09:** `closeVanished` counts good sweeps whose `swept_at` is later than a job's `last_seen_at`. `upsertJobs` stamps `last_seen_at = now()`, and `recordSweep` was called AFTER it — so the sweep that had just SEEN a job counted as a sweep "since it was seen". Every vacancy started at 1, the first absence made 2, and `MISSES_BEFORE_CLOSED` fired a read early: **one paginated hiccup would have withdrawn a live vacancy from the customer portal** and pushed `is_active=false` to prod. Exactly the misleading deletion the module exists to prevent. Fix: **record the sweep BEFORE upserting** (`run_sweep.js`). ⚠️ `jobs_closure.test.mjs` PASSED throughout — it constructs its own rows and never writes the sweep that saw the job, so it tested the SQL while production had a call‑order bug. New `tests/jobs_closure_order.test.mjs` drives the SHIPPED functions in the shipped order against real Postgres; it fails 6/6 on the old code and passes 6/6 on the fix. Nothing was lost — no vacancy has `close_reason='withdrawn'`; it would have bitten on the second nightly. Also fixed alongside: **`jobs.extra_fields` is NOT NULL DEFAULT '{}'** and the new upsert passed an explicit NULL, which would have aborted an entire board's write for any reader that states none.

**🔴 THE JS AND SQL LEGAL‑FORM LISTS HAD DRIFTED — fixed 2026‑08‑09 (migration 113):** the employer matcher's token list was written out twice, once in JavaScript and once in migration 112's index. The JS list carried the SPACED form `w l l`; the SQL list did not. `employerKey` turns "W.L.L." into three separate letters, so **2,188 live companies** normalized to a name ending in a spaced legal form that the CORE key could never strip — a job board saying "Encon Corporation" simply could not find "Encon Corporation W.L.L.", silently, because a miss looks identical to "Bell does not hold that company". Now **one list** (`LEGAL_TRAIL_TOKENS` in `jobs/attribute.js`) builds the JS regex AND the SQL expression, migration **113** rebuilds `companies_employer_core_idx` to match, and two tests assert the SHIPPED index still agrees — one on the tokens, one running EXPLAIN to prove the planner actually uses all three indexes. ⚠️ **Changing an expression index's expression without rebuilding it is silent**: answers stay correct and every lookup drops back to a 2.4 s full scan. Also fixed: matching now tries the EXACT registered name FIRST and only then allows a trailing legal form to differ — folding both into one OR made "Ooredoo" ambiguous (Bell holds "ooredoo" and "Ooredoo Q.P.S.C.") and lost a match that had been right. Precision first, then reach.

**🔴 FOUR MORE CLOSURE DEFECTS, ALL FROM THE ADVERSARIAL REVIEW — fixed 2026‑08‑09:**
- **A poster's expiry date outranked the board that is still advertising the job.** `closeVanished`'s expiry UPDATE was not scoped to vacancies that VANISHED, so **22 of the 233** classifieds postings the site returned in its OWN list of active vacancies were closed on a date their author typed months ago. Worse than the deletion: the next sweep re‑opens them and the one after re‑closes them, churning `updated_at` — the sync watermark — so the flip‑flop republishes to prod on every push. Now an expiry can only close a job that is ALSO absent from this sweep. All 22 are open again.
- **Oracle's `complete` flag was computed and ignored.** A tenant reporting 250 jobs whose page at offset 200 returns empty yields 200 with `complete:false`, and the other 50 look withdrawn. The classifieds crawl got this guard; Oracle did not. It throws now.
- **A QatarEnergy page Bell could not FETCH was treated as a vacancy that vanished** — the comment said the opposite of what the code did. One HTTP 500 in a 21‑minute run withdrew a posting QatarEnergy's own sitemap still lists. Unfetchable ids are now returned as `keepOpen` and counted as SEEN; a **404 remains the only closure evidence** this source gives (proven live on 5 delisted ids).
- **`stage1.js` hard‑deleted mirrored `jobs` rows with no tombstone** — harmless while `jobs` held 87 test rows, live now that it is a customer‑facing dataset. Tombstoned.
⚠️ Also mine, same day: **`packRaw` returns a JSON STRING**, and two readers already call it, so packing again spread the string into a character‑indexed object — `{"0":"{","1":"\""…}` with 5,105 keys. `packedRaw()` detects an already‑packed payload. All 516 board‑read vacancies verified clean.

**📊 MEASURED, so nobody re‑derives it — ALL FOUR SOURCES LIVE:** career portal **249** vacancies, 248 open, **all 249 tied to Qatar Energy** (the posting states its employer; "QatarEnergy" matched the register's "Qatar Energy" through the word‑break pass) · classifieds **233**, 211 open, **111 tied to a company**, 233 naming an employer, 233 locations, 232 employment types, 212 seniority, 225 industries, 68 real descriptions, **22 closed instantly** because their own stated expiry had passed (exactly the "not misleading" behaviour) · Oracle **34**, **0 linked and that is correct** — Oracle states no employer on 86 of 86 requisitions, so there is nothing to match. Across all **516** board‑read vacancies: 0 pointer descriptions, 0 truncated raw payloads. ⚠️ **The own‑site careers reader yields ZERO and that is the finding, not a bug:** across **95** of Bell's own careers pages (25 by id + 70 random), **not one publishes schema.org JobPosting**. 32 of 70 publish other structured data (WebPage · WebSite · BreadcrumbList · Organization · DiscussionForumPosting), so the tooling is there — Qatar employers just do not mark up vacancies for Google Jobs. The reader is kept (correct, free, picks up any site that adds it) but runs **weekly, not nightly** — 255 fetches a night for a measured zero is noise. **Covering every company's own site therefore needs the paid Apify actor Val already anticipated**, or guessing at page layout, which Rule 2.1 forbids.

**PERFECT BELLA — SHIPPED 2026‑07‑20 eve** (deep memory [[bella-perfection]]): silent‑no‑answer/voice‑drop chain fixed (forced honest wrap‑up, streamed fallback, watchdog reset, voice speaks on error); first‑token latency; HONESTY (no false "Done" — forced summary states what did/didn't complete); ORB "working" pulse; BELLA KNOWS THE MAP (map_nearby + land_info tools, 69 tools); voice polish (spoken "working on it" progress, no self‑interrupt, no overlapping turns); plan round‑budget scales to step count + no surprise mid‑plan card. **Remaining Bella polish (low pri):** C4 key‑prewarm (local‑only), C3 atomic tool‑pair, B5 batch‑tools (structural latency cure). **Awaiting Val's test.**

**BRANCH MODEL — SHIPPED 2026‑07‑20 (deep memory [[branch-model]]):** migration 101 `parent_company_id` + exact‑or‑review matcher (`server/enrichment/branch_link.js`) that collapses empty MoPH facility shells into their ONE registered parent (the DOC fix). Word‑preserving key + genericness guard — caught a real Rule‑2.1 bug (stored `name_normalized` is lossy: "Al Jaber Holding Company"→"al jaber" wrongly swallowed "Al Jaber & Partners"). Live: **851 shells → 276 parents** collapse, 62 skipped (generic name, left alone), 0 legal‑branch links (foreign parents). Val's clicks: **Preview Branch Model.command** then **Apply Branch Model.command** (Apply archives shells reversibly + pushes to prod itself). Drawer shows "Branches & facilities (N)" / "Part of a group"; Bella `get_company` now surfaces locations + branches. Independent of the reharvest/geocode steps. Map render fixes + DB‑backed land layer already DONE ([[map-location-quality]]).

**MAP + LOCATION DATA — 2026‑07‑21 (deep memory [[maplink-and-gazetteer]], [[duplicate-locations-bug]]):**
- **DOC IS FIXED.** Root cause was never geocoding: the harvester DISCARDED shortened Google‑Maps share links (`maps.app.goo.gl`) — `harvester.js:253` destructured only `coords`. DOC's site links all 3 branches that way. Now followed → Al Sadd + Lusail Marina + Izghawa live.
- **CAMERA‑vs‑PIN bug (Val caught):** a resolved share link carries `@lat,lng` (map CAMERA) *and* `!3d…!4d…` (the real PLACE). We read `@` → every pin ~259 m off, DOC landed in the wrong tower. Order is now `!3d!4d` → `!3d!2d` → `@` last. 1,347 camera pins deleted locally **and on prod** (12,644 gap tombstones → 1,348 removed).
- ⚠️ **`company_locations` has NO delete trigger** — always write `sync_deletions` BEFORE deleting a mirror row, or prod keeps it forever.
- **Map/UI:** phantom pin piles fixed (one pin per site per website — Yateem 80→5, 371 removed); branch spread is CLICK‑ONLY (Val: never re‑add an ambient network), lines land on the dots, spread outward on click, dark‑cased cyan; globe intro plays once per session; drawer has ONE "Locations & branches" box.
- **GAZETTEER name‑join is PARKED** — its Apply self‑refuses (proof 36%). Do NOT lower the bar: 77% of its yield is one tower (registered‑agent addresses) + confirmed wrong pins.
- **Data‑gap audit + weekly email:** `Data Gap Audit.command` (read‑only) and a Sunday self‑report (`Send Data Check Email Now.command` to test). It already found 387 emails / 420 phones seen‑but‑not‑stored — which turned out to be a **worse** bug than data loss, see next line.

- **OSM Discovery:** auto‑approve command (536 high‑confidence) **plus** "Approve all in category" buttons (Shopping 3,751 · Food & Drink 1,107 · Hotels 882 …).
**🔴 THE LEGACY CONTACT COLUMN — FIXED 2026‑07‑21 (deep memory [[legacy-contact-column]]):** the 387/420 were NOT lost — Bell had correctly DELETED them (contacts harvested from a website that turned out to be a *different* company). But every bulk `DELETE FROM company_contacts` bypasses `deleteContact()`, so **`companies.email`/`phone` kept the wrong address**, and `outreach/targeting.js` UNIONed that column in — stamping `is_verified = true` with no evidence (Rule 2.1) and leaving `email_status` NULL so the bounce exclusion could never fire. **297 role mailboxes belonging to OTHER companies were live outreach targets** (Anya Aviation Consultancy QFZ carried the London handbag brand's `wholesale@`). Shipped: outreach reads **company_contacts only** (7,948 role mailboxes remain — campaign not starved) · `resyncContactColumns()` called after every bulk delete · Bella's CRM send reads contacts first (unblocks 44 "no recipient") · dedup stranded‑children DELETE now guarded like its INSERT · audit no longer reports a deliberate deletion as loss (387→**1**, 420→**3**, both proven‑correct rejections). ⚠️ **Never re‑add a second email source to targeting.js.**

**PENDING (Val's click, no rush) — DISCOVERY REVIEW:** local Portal (127.0.0.1:3939) → nav "Discovery Review" → work the 542 Maps candidates + 110 Qatar Spark discoveries: Approve real Qatar businesses (creates a full company + map pin, dedup‑guarded), Reject junk; 52 foreign are admin‑only (Dismiss). Tell Claude after a batch → Claude pushes. Detail: [[discovery-review-queue]].


**🔴 OUTREACH TIER HOLE — CLOSED 2026‑07‑21 (deep memory [[address-verdicts]]):** migration 095's CHECK allowed `audience_tier` 'unclassified' and 'all', the campaign form OFFERED both, and `targeting.js` read `if (tier !== 'all' && …)` — so an 'all' campaign mailed EVERY tier and an 'unclassified' one mailed 6,330 addresses Bell cannot tie to a company. Now: `buildTargets` throws on any tier but role_mailbox/named_person (`countOnly` keeps the admin summary working), the two options are gone from the UI, migration 104 adds a **NOT VALID** CHECK (NOT VALID on purpose — prod's live campaign row must not abort boot). Also: **a role word on a consumer/ISP domain is no longer a company inbox** — `info@gmail.com` was a live target held against 3 companies. ⚠️ Never re‑add 'all'/'unclassified' as a sendable tier.

**ADDRESS REVIEW — NEW (2026‑07‑21):** resolves the ~6,300 `unclassified` addresses without guessing. 5 signals measured on live data, then attacked by 2 adversarial lenses. **NOTHING survived for the sendable direction** — no rule ever promotes to 'company inbox'; that is always Val's click. Auto (safe direction only): **A1** person linked to this company shares the exact name token → 48 · **A3** website‑template placeholder domain → 38. **300** get a suggested verdict *with the literal proof*; **5,368** stay undecided and out of outreach. Migration 104 `address_verdicts` (MIRRORED — the engine runs on prod, a local‑only verdict changes nothing). Val's path: local Portal → nav **"Address Review"**. Rejected on evidence, do NOT re‑add: inverse name test (would make 4,986 sendable), source‑payload `email` field name (a *negative* discriminator), domain‑echo (24.5% are real given names), fan‑out as an auto block (~52% is Bell's own duplicates).

**📍 LOCATION ROWS — a coordinate is not an address (2026‑07‑21, deep memory [[location-row-merge]]):** when a Google‑Maps link carries no place name the harvester writes the COORDINATE STRING into `company_locations.address`. The UNIQUE key is `(company_id, lower(address))`, so it can never collide with the row holding the real street address for the same doorway — every re‑harvest adds another (DOC: **7 rows for 3 clinics**). Shipped: **display** (`lib/location_display.js` — a coordinate‑shaped address is suppressed in the drawer, company detail, Bella `get_company` and the **physical‑letter generator**, which would have posted an envelope addressed to two numbers; Plus Codes count too — the OLC alphabet INCLUDES W and X) · **harvester guard** (no nameless‑pin row when the company already states a real address within ~22 m — without it, deleting is a treadmill: the next harvest re‑inserts under a new id, proven live) · **Preview/Apply Location Merge.command** (BYTE‑IDENTICAL coordinates only, never a distance threshold; excludes qars winners — QARS returns one point per BUILDING, 60 companies share Ooredoo Tower's; **tombstone BEFORE delete**). Also fixed: branch pins now derive their area from their OWN coordinate (was wrong on 544 of 1,090 — DOC's Izghawa clinic read "Lusail") and a branch pin within 22 m of the company's own pin no longer draws twice (461 stacked). ⚠️ **Killed by adversarial review, do NOT re‑add:** the landmark‑name bridge (pairs neighbours in one tower), text‑normalized dedup (the normalizer strips Arabic to an empty string — 3 sites 53 km apart collided, and it destroyed a verified qars geocode), the INWANI triple as a join key (0 of 537 bare rows carry one).

**📍 LOCATION PAIRS + STATED LABELS — 2026‑07‑22:** Discovery Review has a new **"Location pairs"** tab (16 waiting): a nameless pin beside a surveyed government building whose name appears in the company's own written address — Bell shows the proof (landmark, metres, zone), Val's click merges. DOC leads (Marina 50, 6.8 m). Reject is remembered on the pin row. Also: all four discovery/OSM promote paths wrote `label='Head office'` (a fabricated claim, 534 rows) — now they write the place's own stated name, and the 535 existing rows were relabelled via exact `matched_company_id`+coordinate joins and pushed. `promoteToCompany` no longer defaults city to 'Doha'.

**📍 ADDRESS TWINS + STATED LABELS ROUND 2 — 2026‑07‑22:** the same site written twice splits into MECHANICAL (187 punctuation variants → folded by **Preview/Apply Location Merge.command**, survivor = pinned > primary > older, inherits coords) and JUDGMENT (~100 → new **"Address twins"** tab in Discovery Review — 'Bldg 100' vs 'Bldg 102' is Val's call). Harvester now refuses to mint a wording variant of an address the company already states (normalized ≥12 chars). ⚠️ The RUNNING `Resolve Website Map Links.command` still carries pre‑guard code loaded at its start — counts drift until it exits; the Apply's drift‑refusal protects. **CHAIN LINKING (Yateem):** Val decided LINK‑NOT‑MERGE 2026‑07‑22 ("one organized view, sixteen true records"); adversarial design workflow launched — build lands when its spec returns.

**🔗 CHAIN MODEL — BUILT 2026‑07‑22 (deep memory [[chain-links]]):** Val's decision: LINK, never merge — one organized view, N true legal records, all via `parent_company_id`, one UPDATE to NULL undoes any link. **Tier 1 registry‑stated** (`Preview/Apply Chain Links.command`): the /n suffix on a base CR is the registry's own branch numbering — 1,309 firms / 2,284 links ready; gates: MOCI/QCCI source both ends (QFC/CRA licence numbers collide with CR bases), unique bare‑base parent, never overwrite an existing link; 2,056 groups held for review. **Tier 2 brand evidence** (Discovery Review → **"Chains"** tab, 62 groups): the Yateem shape — head #163975 holds the only CR, 15 no‑CR discoveries share yateemoptician.qa; strangers flagged, nothing links without Val's click. **Dedup guard extended:** sibling branch registrations (…/2 vs …/3) now refuse to merge. ⚠️ The adversarial‑verify phase of the design workflow hit a session limit and DID NOT RUN — that is why nothing auto‑links; do NOT upgrade Tier 2 to automatic without running it (resume: `wf_6857bba6-9aa`, measures are cached).

**🧾 TENDER TWINS — 2026‑07‑22:** the ~80 tenders published on BOTH Kahramaa and Monaqasat now show ONCE (display only, nothing deleted): Monaqasat row leads with an "also on Kahramaa" chip; detail drawer cross‑links both directions; `source=kahramaa` still shows Kahramaa's full 1,765; /stats + status facet count twin‑aware so chips equal list totals (source facet stays raw on purpose). Kahramaa's own `raw.monaqasat_number` is the join — stated verbatim, zero‑stripped compare. ⚠️ **Chain + phone adversarial workflows hit session limits twice** (resets 2:20pm Qatar) — chain refute/spec still UNRUN (resume `wf_6857bba6-9aa`), phone classification 1/6 done (resume `wf_2391735b-3ce`). Nothing shipped depends on them; retry after reset.

**⛓ CHAIN AUTO-LINK IS ON (Val's standing instruction, 2026‑07‑22):** after reviewing the Chains queue Val said *"if CR number is matching let it link automatically."* `autoLinkRegistryChains()` (enrichment/chain_link.js) now runs in every nightly sweep — same Tier‑1 gates (MOCI/QCCI both ends, unique bare‑base parent, never overwrite, only writes when parent_company_id IS NULL — the watermark must not churn). Tier 2 (brand evidence) stays Val's click in the Chains tab. Yateem: 17 branches linked + pushed 2026‑07‑22.

**📮 THE WEBSITE RE‑READ RAN AND YIELDED ZERO — that is the finding, 2026‑08‑11:** `Fix Broken Addresses From Websites.command` read all ~40 broken‑email companies with a source‑stated website through the full harvester (contact pages, /en /ar, render escalation — 1 site needed headless). **Not one publishes an email address.** Verified as a true zero: spot‑fetched three of the crawled sites for `mailto:`/@ — nothing; and the 14 emails that appeared in the same window all came from the ROG's own engines on OTHER companies (checked by cohort id). The sites are as broken as the stored emails — typo'd URLs (`www.qatarnational plasticfactory.net.com`, `ww.hepwortpme.comqa`), dead domains, or brochure pages with no contact — the same registry‑form stratum. Yield that did land: a few phones/socials on cohort companies. ⚠️ **20 of the 60 websites were the Finder's DOMAIN GUESSES resolving to strangers** (tornado.com for "Tornado Trading", vista.com, foodmark.com) — the tool refuses `method='guess'` sites outright; harvesting one would store a stranger's mailbox, the Anya‑Aviation shape. The 246 broken addresses remain HAND‑FIX ONLY ("Bell — broken email addresses.txt" on the Desktop); the tool stays useful for re‑running after the Finder improves sites. Task #96 (guessed sites must NAME the company) got its strongest evidence yet.

**📖 QCCI REFRESH WITHOUT FIRECRAWL — BUILT 2026‑08‑15 (Val: "use ROG… to not use Firecrawl credits"):** qatarcid.com sits behind Cloudflare — plain fetch 403s, the Mac's headless Playwright is CHALLENGED (403 twice, 15 s settle), an interactive browser passes after ~6 s. ⚠️ **The "Click to see" contact gate is THEATRE:** the full value is split across two attributes and the click concatenates them — `data-mxe="4442063" data-mx="4"` → 44420634, `"hassank"+"ousa@live.com"` → the email. No network call (verified by DOM diff), so the reader (`sources/qatarcid/reader.js`) needs no clicking. Parses the fixed label vocabulary (CR Number, QCCI Membership Number, PO Box, Phone/Fax, Contact Person) + breadcrumb taxonomy from BROWSER‑serialized HTML (fixture captured from the pane per §2.2). **Two Rule‑2.1 catches in the first proving run:** the Cloudflare challenge page parsed as a company literally named "www.qatarcid.com" (a blocked night = 2,000 of those into the ingest) — the parser now refuses pages carrying challenge markers or lacking the directory vocabulary; and an **Ashghal SPONSOR BANNER was captured as KBE Gulf's website** — the site now comes only from the sidebar's own url entry, never from generic links. `scripts/qatarcid_recrawl.js`: stalest‑1,000 chunk per nightly (`qcci_recrawl` duty, BDI_QCCI_NIGHTLY_LIMIT, 0 disables; full 41,951 cycle ≈ 6 weeks, free) → writes the June scan‑file shape → the ONE existing ingest (`ingestSource('QCCI')`, whose partial‑scrape guard correctly skips disappearance reconciliation on chunks; June Firecrawl full file backed up once). ⚠️ **PROBES FIRST:** 5/5 unreadable opening pages = this machine's browser is challenged → THROWS with that message, so a blocked night is a red error naming the cause, never "produced nothing" (the Kahramaa lesson). **Whether the ROG's Crawl4AI passes the challenge is UNKNOWN until tonight's nightly runs the probe** — the duties card answers it: green with a number, or red saying Cloudflare. If red: Crawl4AI stealth flags / a headed browser on the always‑home ROG are the next moves. Val's manual chunk: **`Read QCCI Directory Now.command`**.

**🔁 THE DUPLICATE PROBLEM WAS 44× BIGGER THAN THE FINDER COULD SEE — 2026‑08‑13:** the same‑body registry finder ran **'zero' five nights straight** while **6,283 cross‑body duplicate groups** sat in the data, because QCCI writes the CR as `00036876` and CRA as `36876` — an exact‑string, same‑body group can never meet them. The join is proven, not assumed: on the **4,312 companies carrying BOTH a MOCI and a QCCI row themselves, the numbers agree 94.3%** — same national register (company_record/QFC/QFCRA/MoPH number independently and stay excluded). That missing 5.7% is why the number alone NEVER merges: `findCrossBodyBaseCrGroups` merges only three tiers of conclusive evidence — **exact** (normalized names identical, 48) · **shell** (nameless "MOCI CR‑X (name missing)" stub + ONE named row; the named row survives regardless of score, 162) · **corroborated** (pair shares a phone last‑8 or website domain — "Al Wadi Al Akhdar"/"Green Valley Trading" is the same firm TRANSLATED, trigram scores it 0.00, a shared phone proves it, ~200) = **412 conclusive merges**, draining 250/night via the existing nightly registry_merge. **5,871 held** — real danger lives there ("almustaqbal Engineering" vs "Diplomat For Men's Supplies" genuinely share a base CR) and no similarity threshold separates translated‑same from actually‑different; next step is verified expansion, NOT a threshold guess. ⚠️ Also fixed the same day, found by the audit: **mergeCompanies stranded `company_relationships`** (367 rows, 61 of which would become self‑loops if re‑pointed blindly — intra‑pair edges are deleted with tombstones, outgoing edges collision‑checked against the name‑keyed unique index, everything stamps `updated_at` because the table is mirrored) · **305 pre‑existing self‑loop edges** (a company partnered with itself) queued for deletion in the heal · **PROD's tenant tables were re‑pointed by NOTHING** — CRM records, paid reveals, list members, contributed datapoints live only on prod, aren't mirrored, and a merge physically tombstones the duplicate's contacts off prod while the tenant's record stays frozen on the archived shell. New `sync/reconcile_merged.js` runs on prod after every push (route `POST /api/sync/reconcile-merged`) + locally, following `companies.canonical_id`; **collisions (a tenant tracking BOTH duplicates) are reported, never resolved** — resolving one deletes a record carrying the tenant's own notes. `credit_ledger` is deliberately NOT re‑pointed: a ledger records what was bought, and rewriting it is falsification, not repair. **Registry staleness verified 2026‑08‑13:** MOCI newest 2026‑06‑11 (133,357 rows) · QCCI 2026‑06‑19 (41,951) vs QFZ/QSTP/QFC all 2026‑08‑07 (the Saturday scan covers only those three). MOCI's stage‑1 parser is FINISHED (scraper.py:916 says only Stage 2 is not); scheduling it on the ROG's Saturday task is the open item. QCCI sits behind Cloudflare — Firecrawl ~42k credits/run, Val's call.

**☀️ VAL'S LIST AFTER 2026‑08‑11:**
1. **`Apply Unsendable Address Marking.command`** (~10 s) — marks the 246 broken addresses invalid so Bell stops offering them as recipients. Nothing deleted, nothing rewritten; the Desktop list stays valid for hand‑fixing.
2. **Staging decision** — measured: staging Postgres **6.63 GB storage / 5.24 GB RAM continuously**, has not gated a release since 2026‑07‑11 (254 of 258 deploys reached prod within 60 s of staging; median 2 s). `BDI_OPEN_DATA_SYNC=0` is set on the 3 staging services (stops growth, reclaims nothing). Deleting the environment is the real saving; `Redeploy Bell Services.command` already tolerates its absence. Order if Val goes ahead: copy each staging service's Variables (Raw Editor) into a note OUTSIDE the Bell folder → disable the app‑staging webhooks in Clerk + Stripe (disable, not delete) → Railway → Settings → Environments → delete `staging` → remove the 3 staging CNAMEs at the DNS host. Bring‑back ≈ 1 hour with the saved variables.
3. Discovery Review queues: **Hiring 48 left** (C2HR proven: → company #197453‑era flow, 10 jobs attached on click) · Chains 21 · Location pairs 74 · Address twins 300 · Maps · Spark Qatar 217. Address Review (300). The Desktop list "Bell — broken email addresses.txt" (155 companies with no website = hand‑fix only), whenever.
4. ⚖️ PDPPL lawyer review of cold outreach — still open.

**Previous list (2026‑08‑09), kept for context:**
1. **`Find Production Orphans.command`** (workspace root, READ‑ONLY, ~2 min) — lists the **10,325 rows the live site still shows that Bell deleted**: person_companies 10,026 · company_tech 240 · **company_contacts 41** · people 7 · knowledge_sources 6 · person_contacts 5. The 41 contacts are the ones that matter — deleted *because they belonged to a different company*, still live and revealable. If it reads right, then **`Apply Production Orphan Cleanup.command`** (types `yes`, queues the removals, pushes). Nothing Bell still holds is ever touched.
2. Local Portal (127.0.0.1:3939) → **Local Engines** → new **"Scheduled duties"** card. Green = ran and produced; amber = ran and produced nothing; red = failing or **"started but never finished"**. Tonight's nightly should turn `tender_scan` / `award_reports` / `weekly_data_check` green — they are the three still showing 40h+.
3. Local Portal → **Jobs** — now **603** vacancies from four sources. A row with a plain (non‑blue) company name marked **"as advertised"** is a real vacancy whose employer Bell holds no record for; that is correct, not a gap.
4. **`Read Job Boards Now.command`** whenever you want a fresh read on demand (~25 min; the national portal is the slow part, read at the 5 s delay its own robots.txt asks for).
5. Still open from before: Discovery Review queues (Chains · Location pairs · Address twins · Maps · Spark), Address Review (300), and the ⚖️ PDPPL lawyer review of cold outreach.

**🏁 THE MACHINE IS COMPLETE — 2026‑07‑23 night (deep memory [[two-machine-plan]]):** seven \Bell\ tasks green on the ROG: Crawl4AI service (Interactive logon, browser at the SHARED C:\bell-ops\ms-playwright — Claude-on-Windows AppData is SANDBOXED and invisible to real tasks), Continuous Sweep, Nightly 00:30 (self-update + tender scans + chain auto-link + freshness cycle), Push 07:00/19:00, Spark 10:00, Geocode Sun 09:00, **Registry Scan Sat 08:00** (QFZ→QSTP→QFC-detail → `scripts/ingest_source.js`; QFC validation added +176 real companies). Scraper CODE now versioned in git (Data/ output stays ignored) with UTF‑8 baked in. MOCI stays manual (parser unfinished). **Pending Val (20 s):** `netplwiz` auto‑login so reboots self‑recover; until then a reboot parks only Crawl4AI. **Wave 2 queued:** Other Sources (QSE · Qatar Chamber/QCCI · MoPH · Tasmu · CRA ICT · MadeInQatar · QFCRA) — validate then schedule monthly.

**🖥 THE FLIP HAPPENED — 2026‑07‑23, ~01:15 (deep memory [[two-machine-plan]]):** THE database now lives on the ROG (192.168.1.161, PostgreSQL 18.4 Windows build — verified via inet_server_addr). The Mac reads/writes it through `Portal/server/.db-target` (gitignored; delete that file = instant rollback to the Mac's local DB, which is intact and frozen as the backup of 2026‑07‑22 20:43, alongside the .dump on the Desktop). Mac's three LaunchAgents (continuous-engine, harvest-sweep, crawl4ai) are UNLOADED PERMANENTLY — engines run only on the ROG. Every Mac .command file and the Mac Portal now operate on the ROG data transparently. The ROG smoke test caught a REAL harvester crash (companyId→company.id, commit 6947033 — shipped 2026‑07‑22 without end‑to‑end execution; the lesson is in memory) and exposed that the Playwright renderer needs its own install on Windows. ⚠️ When the Mac is away from home Wi‑Fi, local-data work waits (app.bell.qa still viewable); Tailscale is the optional later fix.

**🖥 TWO-MACHINE PLAN AGREED (2026‑07‑22, deep memory [[two-machine-plan]]):** Val's ASUS ROG (Windows, 16 GB) becomes the always-home engine room — THE database moves there, all engines + scheduled prod pushes run there 24/7; the Mac becomes the control screen (Portal via browser at the ROG, .command files repointed). Rationale: Mac restarts/leaves home; one DB on the always-home machine = nothing to sync. Plan doc at repo root: `Bell — Two-Machine Plan (Windows Engine + Mac Control).md`. Val's Phase 1: install Node/Git/PostgreSQL 16/Chrome/Claude Code on the ROG + never-sleep power settings. Phase 2 (move night) is guided; `Export Database for Windows.command` exists. ⚠️ Until the move happens, NOTHING changes — the Mac stays source of truth and publisher.

**VAL'S PENDING STEPS (as of 2026‑07‑20 eve — all Bella + map + DOC‑safe fixes tested PERFECT by Val):**
- [ ] **a.** `Reharvest for Locations.command` is RUNNING (9,689 website companies incl. DOC, captures branch addresses + Google‑Maps‑link exact coords; hours, resumable). ⚠️ Do NOT run Spark or Geocode alongside it (8 GB Mac). When it finishes → Val pastes the closing summary → **Claude runs the data push himself** → then Val runs `Geocode Companies.command`. THAT is when DOC's website branches land on the map.
- [ ] **b.** `Run Spark Enrichment.command` (daily) — Val runs later; expect batch holds ~12, no "max credits". Paste closing lines.
- [ ] **c.** BRANCH MODEL — ✅ APPLIED by Val (851→276 collapse live). PLUS the "robust + awesome" upgrade DEPLOYED ([[branch-model]]): **map branch network** (always‑on parent→branch tie‑lines, lights up fully after Reharvest+Geocode) + **branch contact rollup** (Preview/Apply Branch Contact Rollup.command — rolls each operator's unique branch emails/phones onto the parent; venue‑domain + Qatar‑phone guards; 12→4 clean; Val's optional click, small yield now).
- [ ] **d.** Remaining safe DOC cleanups — ✅ mostly SHIPPED 2026‑07‑20 (deep memory [[data-quality-cleanups]]): **dedup registration‑conflict guard** (branch‑stripped base CR — 42828/2 still merges into 42828, only distinct base CRs blocked; belt inside mergeCompanies, all 4 paths) DEPLOYED, protects the next dedup run automatically. **Junk‑address forward guard** (guessAddress now delegates to the guarded extractor; isJunkAddress on contributions) DEPLOYED. **City guesses stopped at source** (QFC no longer hardcodes 'Doha'; Stage‑2 no longer writes LinkedIn HQ city) DEPLOYED. Junk‑address cleanup **APPLIED by Val** (236 cleared). **QFC "Doha" cleanup BUILT + DEPLOYED** (Val greenlit): **Preview/Apply QFC Doha City Cleanup.command** — corroboration‑guarded, clears only the 80 of 5,243 where nothing (coord/address/other source/branch) confirms Doha; several are actually Lusail. Awaiting Val's Preview→Apply click. **OSM INGEST — ✅ BUILT + DEPLOYED 2026‑07‑21 ([[osm-ingest]]):** migration 102 `osm_places`+`osm_streets`, area‑filtered Overpass engine (excludes Bahrain), `/api/osm/*` routes, Bella `search_places` + `map_nearby` places, **"Places (OSM)" map layer**, company dedup‑link. **Val's action:** double‑click **Ingest OpenStreetMap Qatar.command** to populate (fetches every named business/restaurant/shop/clinic/hotel/street in Qatar + pushes; don't run alongside a harvester).
- [ ] **e.** **`Preview Legacy Contact Repair.command`** → then **`Apply Legacy Contact Repair.command`** (workspace root). Fixes the wrong‑company addresses left on the legacy column. Preview shows: **promote 126** role mailboxes an official source (QCCI/Tasmu/QSTP) states verbatim for that company · **clear 373 emails + 409 phones** that Bell already deleted as another company's (removed value kept in `extra_fields.legacy_contact_removed`, nothing destroyed silently) · **leave 233** alone (49 named persons = PDPPL, 150 unconfirmable, 24 junk). Seconds, not hours. Claude pushes to prod after.
- [ ] **f.** **`Preview Address Auto-Decide.command`** → **`Apply Address Auto-Decide.command`** (workspace root): records the 86 safe verdicts (48 people, 38 placeholder domains). Then local Portal → **Address Review** to work the 300 suggested ones — each shows Bell's proof and a suggested answer; "Company inbox" is never bulk‑appliable because that is the verdict that lets Bell email them.
- [ ] **g.** **`Preview Location Merge.command`** → **`Apply Location Merge.command`** (workspace root): collapses 5 rows that state only a coordinate another row already states with a real address (DOC is one). Seconds. No map pin changes — both rows sat on the same point.
- [ ] **h.** Local Portal → **Discovery Review** → **"Location pairs"** tab (16 cards): each shows a pin, the surveyed building at that exact spot, and the company's own written address that mentions it. Click **"Same place — use this address"** if they match (DOC's Marina 50 is the first card), **"Not the same"** otherwise. Tell Claude after — he pushes.
- [ ] **i.** AFTER `Resolve Website Map Links.command` finishes: re‑run **`Preview Location Merge.command`** → **`Apply Location Merge.command`** (it now also folds the 187 punctuation‑twin addresses; run it after the resolver exits or Apply will refuse on drift — that refusal is correct, just re‑Preview). Then the **"Address twins"** tab in Discovery Review (~100 judgment cards, no rush).
- [ ] **j.** **`Preview Chain Links.command`** → **`Apply Chain Links.command`** (workspace root): links 2,284 registry‑numbered branch registrations under their 1,309 parent firms (the /n CR suffix is the registry's own statement). Then Discovery Review → **"Chains"** tab: Yateem is the first card — "Link all 15 as branches" is the click that makes it ONE family. Claude pushes after.

- [ ] **1.** `Reharvest No-Email Companies.command` — was RUNNING 2026‑07‑19/20 (the ~8,600 harvested‑but‑no‑email cohort through the upgraded extractor; hours, resumable). Val will paste the closing lines ("Cohort: X → Y"). When done: **Claude runs the DATA push himself** (runPush) — do not send Val to `Push Changes.command` for data. (Interim pushes are fine and have run — 2026‑07‑20 morning push: 98 rows, 0 errors.)
- [ ] **2.** After step 1: `Geocode Companies.command` once more — newly harvested structured addresses convert the "unparseable" pile into map pins (first runs: 11 → 410 coords, 7.3k unparseable awaiting better addresses). It pushes to prod itself.
- [ ] **3.** THE OUTREACH MACHINE IS ARMED (BDI_OUTREACH_SCHEDULER=1 + BDI_OUTREACH_ENABLED=1 on app.bell.qa) and the **"Bella outreach" campaign is ACTIVE with MyWeb Systems** — first real cold send lands the next Qatar working window (Sat–Thu 07:00–17:00). Val checks admin.bell.qa → Marketing → Stats/Mail log. Weekly digest auto‑sends Sundays ≥09:00 (confirmed firing).
- [ ] **4.** Ramp‑up to the full ~5,400 role‑mailbox campaign: plan approved (tender‑heavy industries first — prioritization is coded into Plan), waiting on a few days of MyWeb/engine results before Val activates a bulk campaign.
- [ ] **5.** Eid al‑Fitr / Eid al‑Adha dates → add to the holiday calendar when Qatar announces them (admin → Marketing API or ask Claude).
- [ ] **6.** ⚖️ The PDPPL lawyer review of cold outreach is STILL OPEN (Val's standing instruction: machine runs on his authority; brief at repo root `Bell — Legal Brief for Counsel (Qatar PDPPL outreach).md`).

**Not urgent, whenever he has a spare night:**
- [ ] `Enrich Ashghal Details.command` — closed/archived Ashghal tender details. Browser‑based, hours; not alongside another long enrich.
- [ ] 271 tender rows "awaiting host heal" → `Backfill Full Tender Archive.command` then Preview/Apply Tender Phantom Repair.
- [ ] `Apply Website-Content Conflict.command` (engine paused) — cleans existing wrong‑content rows (QF Endowment etc.); Preview showed 0 false positives.

**Parked at Val's request:** MOCI Stage‑2 (design in `Bell — MOCI Stage-2 Design (Phase 2 A1).md`) · Hukoomi KB source (needs a watched browser run) · marketing‑site SEO validation (waiting on Google) · physical‑letter generator · Arabic outreach campaigns (one flag away).

---

## 1. What Bell is

**bell.qa** — a Qatar business‑intelligence platform. It gathers company, people, tender and signal data from Qatar sources, enriches it, and sells access (search, reveal‑to‑CRM, signals, research, outreach) to paying tenants.

**One codebase, three deployments** (differ only by `BDI_MODE`):

| Deployment | Where | `BDI_MODE` | Role |
|---|---|---|---|
| Local engine | Val's Mac, `127.0.0.1:3939` | `local-admin` | ALL heavy work: scraping, enrichment, dedup, assembly. No auth. Pushes data up. |
| User portal | `app.bell.qa` | `user` | Customer SaaS. Authenticated, tenant‑scoped. No heavy ops. |
| Admin portal | `admin.bell.qa` | `admin` | Platform admin. Read‑only for canonical data. |

Plus a separate Next.js marketing site (`bell-marketing/`) at `bell.qa`.

Stack: Railway (compute + Postgres) · Cloudflare · Clerk (auth) · Stripe · Resend. **Not Vercel.**

Prod is a **full id‑based mirror** of local. The local engine pushes; Railway computes nothing. Hard deletes propagate via `sync_deletions` tombstones → `POST /api/sync/delete`.

---

## 2. Non‑negotiable rules

### 2.1 Never guess. Ever.
**If the source doesn't state it, Bell doesn't claim it.** No inferred units, no derived industries from a buyer's department, no "probably". A missing value must stay missing. An unknown option must **fail loudly**, never fall back to a destructive default.

This rule has been violated four times and each time it corrupted production silently:
- A parser asserted `contract_days` when the page printed a bare `3` with no unit.
- A rescan route did `SCOPES[scope] || SCOPES.all` → re‑queued every engine for every company, nearly burning ~120k paid Firecrawl credits.
- A card parser guessed a detail link by array position → attached the wrong tender's data.
- A regex scanned forward from a table header and captured the **next header** (`entity_ref` = the literal string `"Request"`).

### 2.2 Prove parsers on live data before shipping
Run the exact logic against the real page and show `X/X correct` against ground truth. **Verify against BROWSER‑serialized HTML, not `fetch()` HTML** — they differ (entities decoded, `&#xD;&#xA;` becomes a real newline). A line‑position parser once scored 12/12 on fetch‑HTML and 6/12 on the browser HTML production actually uses.

### 2.3 Test discipline
- `node --check` every edited JS file.
- **PGlite** (`@electric-sql/pglite`) for any SQL logic, applying the real migration files.
- Unit tests against real captured fixtures. Existing suite: `server/tests/tender_phantom_split.test.mjs` (55 tests) — run it after touching tenders.
- `node --check` does **not** catch React hook order. See 2.6.

### 2.4 Never truncate serialized JSON
Use `server/tenders/raw.js` → `packRaw(raw)`. `JSON.stringify(x).slice(0, N)` produces invalid jsonb, Postgres rejects it, the row's catch swallows the error, and the record is silently lost.

### 2.5 8 GB Mac — memory discipline
Val's machine has 8 GB. `ramSafeConcurrency()` caps tender scraping to **2** concurrent renders. Crawl4AI recycles its browser every 150 pages.
**Never run two enrichment engines at once.** Before any long tender enrich, tell Val to pause the always‑on engine (local Portal → Local Engines → Pause). A singleton guard exists in `continuous_sweep.js` but cannot stop already‑running processes.

### 2.6 React hooks above every early return
All `useState`/`useEffect`/etc. must precede any `return` in a component, or the page blanks with no error. Grep hook placement after editing UI. (The other blank‑page cause is stale JS cache — no‑cache headers in `server.js` fix it.)

### 2.7 PDPPL
Qatar's data‑protection law governs anything touching personal data. People data is **locked from customers** (admin keeps access). Registry people (board/partners/signatories) are PDPPL‑sensitive — **ask Val before wiring them anywhere.**

### 2.8 Model rules
Bella = `claude-sonnet-5`, **never** send `temperature` (HTTP 400), **never** Fable or Opus for Bella. News + marketing Bella = `claude-haiku-4-5`.

---

## 3. How Val works

**Click‑only.** He does not use a terminal. Every operation is a double‑click `.command` file in the workspace root, or a button in the local Portal. Long runs must be **resumable** — he will close the window.

When you build something he must run, you build the `.command` for it.

**Deploy** = `Push Changes.command` (→ staging) then `Open Production Release.command` (→ prod). **Always both environments**, never one alone. Deploys are **batched to the end of a phase** — build and verify locally through the whole phase, then ship once.

**⚠️ Do not run `git` against the mounted workspace folder from a sandbox** — it leaves a stale `.git/index.lock` that breaks his Push (fix: `Fix Git Lock.command`).

### If you have git access (Claude Code)

**⭐ THE REPO HAS EXACTLY TWO BRANCHES, FOREVER.**

| Branch | Meaning | Who pushes |
|---|---|---|
| `develop` | staging — Railway auto‑deploys to the staging services | you, freely |
| `main` | **production** — Railway auto‑deploys to app.bell.qa + admin.bell.qa | Val, or you only when he says so **in that session** |

- **Never create a branch.** No `feature/…`, no `fix/…`, no `claude/…`, no date‑stamped branches, no worktree branches. Commit straight to `develop`. Val has said branch sprawl from previous Claude Code sessions was a real problem; two clean branches is the permanent state.
- **Never `git checkout -b`, `git branch`, `git switch -c`, or `git worktree add`.** If you think a branch is warranted, stop and ask Val first.
- **Never delete, rename, force‑push, rebase, or reset** any branch. No `git push --force`, no history rewriting.
- **Deploys (Val's standing rule, 2026‑07‑11): Claude deploys BOTH environments itself — always together.** Push `develop` (→ staging), then fast‑forward production: `git fetch origin main && git merge origin/main --no-edit && git push origin develop && git push origin develop:main`. Never force‑push; if `develop:main` isn't a fast‑forward after merging origin/main in, STOP and investigate. Only deploy work that is tested + verified (the 100% bar is unchanged — this changes WHO clicks, not what qualifies).
  ⚠️ **Reversion condition (Val's words):** once Bell is complete and has PAYING USERS, production goes back to manual — Val tests on staging, then merges to prod himself so users are never affected. When that day comes, restore the old rule here.
  `Push Changes.command` / `Open Production Release.command` remain Val's click‑path for when he deploys himself.
- Stay on `develop`; `git rev-parse --abbrev-ref HEAD` must always print `develop`.
- Never commit secrets. `.env`, keychain values, `BDI_SYNC_TOKEN`, API keys — none of it goes in a commit.
- If you find a stray branch, **report it to Val, don't delete it yourself.**

Also: don't run `git` from a sandbox against this folder (stale `.git/index.lock`). Running `git` natively in Claude Code on Val's Mac is fine.

### End every turn with
1. **Numbered steps for Val**, in order, each naming **where** it happens — which `.command` to double‑click, which portal (local `127.0.0.1:3939` vs `app.bell.qa` vs `admin.bell.qa`), which environment. He does not know where things live; spell it out. One action per step.
2. **What to test**, and what "correct" looks like (the exact number or screen he should see).
3. A short commit message — only when it's actually deploy time.
4. **Questions, separated at the end**, never mixed into the steps.

**Instruction rules for Val (he is not a developer):**
- Never hand him a shell command to run. If something must run, **build a `.command` file for it** and tell him to double‑click it.
- Never say "just run the tests" or "commit this" — either you do it, or you give him a double‑click.
- Long operations must be resumable and must say roughly how long they take, so he can walk away.
- When a run finishes, tell him exactly which output line proves it worked.
- If a step is destructive or spends money, say so plainly **before** the step, and give him a preview/dry‑run first (that is why every repair tool ships as `Preview …` + `Apply …`).

Be honest about what is proven vs assumed. A past audit produced **four false "gap" claims** — treat audit‑style assertions as suspect until code‑verified. Say "I have not verified this" rather than implying you have.

---

## 4. Repo map

```
Portal/
  server/
    tenders/        scrape_monaqasat|ashghal|qatarenergy, ingest, enrich, match, raw, push_prod
    enrichment/
      local/        harvester, finder, relationships, email_finder, company_facts,
                    tech_stack, crawl4ai, render, http      ← Engines 1–6, all $0
      stages/       stage1..6  ← EXTERNAL, PAID (Apify, Firecrawl), manual‑only
    bella/          brain, tools (~57), prompt, store, scheduler, voice, marketing
    news/           signals.js (signal generators), enrich.js (Haiku summaries)
    routes/         express routers
    scripts/        one‑shot + diagnostic scripts run by .command files
    tests/          node test files (run with plain `node`)
  migrations/       NNN_name.sql, applied in order at Portal boot. Latest = 102.
  ui/components/    React 18 (esm.sh import map) + htm tagged templates. No build step.
Data/Companies/1. Data Gathering/Directories/   MOCI, QFC, QFZ, QSTP scrapers (Python)
*.command                                        Val's entry points
```

**Key gotcha:** `db.js` installs a global int8 → JS Number parser. Do not remove it or id comparisons break silently.

---

## 5. Current state (2026‑07‑19)

### Live (everything committed IS deployed — no uncommitted work; tree = both envs)
~27K tenders across FOUR sources (Monaqasat + Ashghal + QatarEnergy + Kahramaa) · signals + QSE disclosures + Qatar Market Pulse · Qatar Knowledge Base (governance + laws + regulators + Gazette feed, customer‑facing) · GIS/Real‑Estate on the Map · Bella (chat + voice, ~57 tools incl. add_to_outreach + get_outreach_status, platform‑admin gated) · Team/CRM/credits/0‑Risk · saved lists + notes.

**THE SELF‑MARKETING MACHINE (2026‑07‑18/19, ARMED + LIVE):** full autonomous cold‑outreach engine at admin.bell.qa → Marketing. Isolated go.bell.qa Resend channel (never falls back to transactional) · consent ledger + one‑click unsubscribe (/u/:token) + suppression manager · Haiku composer (no em‑dashes, no AI clichés, grounded) · warmup ramp (actual send‑days), Qatar hours + holidays, per‑domain throttle, circuit breaker (any complaint / >5% bounce = self‑pause), pre‑flight self‑test · follow‑ups (3 touches, sticky to Thompson‑bandit A/B arms on reply rate) · reply intelligence (IMAP poller on replies@bell.qa; quote‑stripped classification — the footer's own "Unsubscribe" word once made EVERY reply look like remove_me; "remove me" EN/AR = auto‑unsubscribe; interested = 🔥 hot lead + forward to hello@bell.qa) · conversion attribution → "Converted" · **email observatory** (migration 097 email_log at the sendEmail chokepoint — every send from every Bell system counted, all statuses) · weekly market‑updates digest (real tender/signal numbers) + welcome email for bell.qa/market-updates subscribers · contact form live at bell.qa/contact. Campaign "Bella outreach" ACTIVE (MyWeb Systems pilot). The Plan button orders targets tender‑heavy‑industries‑first. Lawyer review OPEN (Val's authority; brief at repo root).

**DATA COMPLETENESS Tracks A+B (2026‑07‑19, DEPLOYED):** harvester upgrade — role emails on external domains KEPT (Doha Clinic case → info@dchqatar.com captured, proven live), Cloudflare/entity/at‑dot de‑obfuscation, WhatsApp contacts (type 'whatsapp', shown in UI), locations/branches + /en /ar pages, per‑page render escalation, multi‑address capture, doctors/staff (admin‑locked via the existing people lockdown) · migration 098 `company_locations` (mirrored) · **$0 QARS geocoder** (Qatar's own GIS locator, INWANI zone/street/building codes, EXACT‑or‑nothing, built‑in ground‑truth proof pass that refuses to run under 85% agreement) · branch pins + sibling tie‑lines on the Map · "Locations (N)" drawer block · Bella get_company includes locations/WhatsApp. Val's commands: `Reharvest No-Email Companies.command` (the 8.6k no‑email cohort) + `Geocode Companies.command` (first runs took the map from 11 → 410 coordinates).

**BELL SCORE v2 + FIND-ANYTHING SEARCH (2026-07-20):** score formula rebuilt (19 components = 100; now counts WhatsApp/locations/coordinates/financials/tech/ownership) and made LIVE — every enrichment writer rescores per record, nightly_sweep heals drift (scoped `IS DISTINCT FROM` — never touch updated_at needlessly, it's the sync watermark), one-time rescore corrected 189,928 companies + 28,526 people (people scores had been frozen 5 weeks; "pharmacy" search had returned all of Healthcare; "IT" had returned 73% of the DB). Business-type search: `lib/business_types.js` bridges user wording to STATED vocabulary (QCCI sub-categories in companies.sector — 571 fine trades, industries[] tags, extra_fields.gmaps_categories) — "haircut salon" → Ladies Beauty Saloons (338), "gift shop" 64→527, name searches unaffected; matched-type chips + Business-type facet in Companies UI; Bella benefits automatically. Deep memory: [[bellscore-and-search]].

~191K companies, ~76K active, ~16K with websites. Latest migration = **115**. Deep memory: [[self-marketing-outreach]], [[data-completeness-program]], [[bellscore-and-search]].

### ⏳ Immediately pending — see the OPEN CHECKLIST at the top
Reharvest running → Claude runs the DATA push himself → Geocode again → watch MyWeb first send → ramp decision → lawyer.

---

## 6. The plan (6 phases, Val green‑lit)

1. **Tenders → buyer‑intent signals** — ✅ done. Remaining: `#73` auto‑scan scheduler (LaunchAgent, daily, all sources), parked until Val has watched signals for a few days.
2. **Data maximization** ← *current phase.* Engine 6 done. **A3 proof‑of‑search ledger + C1 QSE disclosures: BUILT 2026‑07‑10** (in the tree, undeployed — see §5). **C2 DONE 2026‑07‑12: Qatar Market Pulse** — trade flows (1.08M import + 94K export records → monthly QAR series + top partners), weekly real‑estate transactions, MOCI licence issued/canceled dynamics; derived by pure SQL over od_records (every number a sum/avg of source‑stated figures, sources + sync dates attached), 6h process cache (~3.3s cold), `/api/open-stats` + Market Feed 'Qatar Market Pulse' panel + Bella `get_market_stats` (54 tools). Phase 2 is COMPLETE except parked MOCI Stage‑2. **C4 DONE 2026‑07‑12: Kahramaa is tender source #4** (km.qa BusinessWebService ASMX, plain fetch — full archive 1,648 tenders + 8 award categories with winners/amounts; 1,968 rows live, 99 winners linked; `Run Kahramaa Scan.command`; MonaqasatNumber cross‑ref captured verbatim, ~90 true overlaps with our Monaqasat rows — display dedup TBD with Val). **MoT half of C4: no dedicated tender feed exists** — mot.gov.qa publishes via the central Monaqasat portal, which Bell already scrapes; closed honestly. **MOCI Stage‑2 is PARKED** at Val's request (the diagnostic run was painful); the design lives in `Bell — MOCI Stage-2 Design (Phase 2 A1).md`.
3. **Bella as the brain of the business** ← *current phase, batch 1 BUILT + live‑verified 2026‑07‑11 (on develop, not on prod):* fill‑in fixed end‑to‑end (honest tool results — fill_field never claims success; failed fills show a red toast; data‑bella‑fill on Settings/ICP fields; Enter‑commit for chip fields; checkbox support; navigate now reaches Settings sub‑pages e.g. the ICP form; update_icp/update_account_prefs refresh the on‑screen form — the silent Save‑revert trap is gone) · speed (4th prompt‑cache breakpoint on conversation history — measured 94% cache read on turn 2; tools in a round run concurrently; cache‑hit % logged per turn) · awareness (get_tenders: q/industry/source/buyer/ICP filters + deadlines; NEW get_disclosures for QSE events; 50 tools total). Batch 2 SHIPPED 2026‑07‑11 (both envs): **plan bundles** (propose_plan — one card for a whole multi‑step job; approval grants that turn a per‑tool budget counted from the card's own steps; live‑verified end‑to‑end) + **sentence‑streamed voice** (first sentence goes to TTS while the reply still generates; barge‑in stops queue + aborts the turn; ui/lib/speech.js pure segmenter). Also: durable approvals inbox + orb badge (voice/chat/reload‑safe — fixed the lost‑card bug Val hit). Batch 3 SHIPPED 2026‑07‑12 (both envs): **proactive awareness** (a one‑line fresh‑signals brief with ICP matches rides every turn — live‑verified: Bella opens conversations with what matters) + **show_tenders/show_signals** (she drives the Signals radar + embedded Tenders browser like Companies; live‑verified in the browser) + **Arabic voice** (TTS pins language_code=ar on Arabic replies; optional BDI_BELLA_VOICE_ID_AR voice). Phase 3 is feature‑complete pending Val's full test round; 'onboarding — Bella does it for me' moves to Phase 4.
4. **Signup & onboarding** — more signup fields (QID is PDPPL‑sensitive, confirm with Val), guided visual setup, % completion, "Bella does it for me".
5. **Team** — Clerk Organizations. Backend foundation exists but is dormant (migration 027). Unbuilt.
6. **Bell as a business** ← *current phase; multiple batches SHIPPED both envs 2026‑07‑12/13.* **Economics cost dashboard DONE** (migration 085 operating_costs + routes/economics.js + EconomicsTab, admin). **Qatar GIS / Real Estate DONE** (ArcGIS ingest, buildings on the main Map, cadastre parcels, transactions). **Qatar Knowledge Base DONE (Batches A–D)** — Bell learns Qatar governance + LAWS from official sources, cited: migrations 086+087, `server/knowledge/{crawl,crawl_almeezan,entities}.js`, **Al Meezan** laws (resumable TLS‑relaxed id‑walk, EN+Arabic, validated‑law‑only), entity extraction (laws/bodies/amounts/officials w/ verbatim proof, PDPPL‑safe), Bella `search_qatar_kb`, customer‑facing **Qatar Knowledge** browse section (`routes/knowledge.js` + `KnowledgeTab.js`, search/filters/drawer/Recent‑updates), `Run Qatar Knowledge Scan.command`. 32 unit tests + PGlite + live browser‑verified; hardened via adversarial review (10 defects fixed). Val test guide: repo root `Bell — Qatar Knowledge Test + Run Guide (2026-07-13).md`. **Hukoomi (fees/processes) PARKED** — Cloudflare + Sitecore/Next.js SPA needs a Crawl4AI browser batch Val watches (`Bell — Hukoomi Source (Phase 6 KB) — Recon + Plan.md`). Remaining Phase 6: **competition watch · moat strategy · Official‑Gazette new‑law signal · Tier‑3 regulators (QCB/tax/labour/PDPPL/QFC/QFMA/CRA) · MoPH licensing**. See memory [[qatar-knowledge-base]], [[phase6-bell-as-business]].

**Gate:** automated outreach stays off until the database is certified trustworthy (Val's rule).

Rolling/parked: self‑marketing decisions + PDPPL lawyer · 0 Risk staging test · Import‑2 user testing · Research engine (Firecrawl Spark broken, returns `data:null`) · Val's queued UI comments.

---

## 7. Source quirks worth not relearning

- `jsonb_exists(raw,'key')` is **TRUE when the value is JSON `null`** — the key exists. This looped 1,774 tenders forever as "pending".
- `pgrep -f <script>` also matches a `caffeinate` wrapper carrying the same path. Filter on the real process name.
- launchd agents **cannot use paths inside `~/Desktop`** → `EX_CONFIG (78)`, the job never runs and writes zero logs. Logs live in `~/Library/Logs/bell-qa/`.
- Monaqasat `value_amount` is the **tender bond**, not the contract value. Its "sector" field is a bidder *type* ("Suppliers"), not an industry.
- Monaqasat 5‑digit activity codes are real ISIC classes; low divisions arrive zero‑padded to 6 digits.
- Monaqasat card titles **embed the buyer's own reference** (`… - LTC-2417/2025 - …`). Split cards only on a ref alone on its own line.
- Detail pages are **header/value tables**. Parse `<td>` cells (`detailFields(html)`). Never scan forward from a label.
- Ashghal lists paginate by plain GET `?PageIndex=N`, not `__doPostBack`. Its awarded page publishes **winner + all bidders + ICV score**.
- ⚠️ **CORRECTED 2026-08-05 — "Monaqasat hides the winner" was WRONG** and cost Bell a year of award data. `monaqasat.mof.gov.qa/TendersOnlineServices/AwardedTenders/{page}` lists awarded tenders (~1,187 pages), and each row links `/TendersOnlineServices/TenderCompaniesDetails/{id}`, a plain-fetch `<td>` table that states **Awarded Date, Awarded Amount (QAR), Company name, Commercial Registration Number, Approved Value, Financial Result** and ICV. Verified live on two tenders: 4905/2022 → ALMOHANNADI FOR ROADS AND TRADING AND CONTRACTING, CR `71717 | 107026`, QAR 105,763,640.26; and 1799/2023 → AL ALI ENGINEERING CONTRACTING AND TRADING, CR `46694 | 71957`, QAR 402,500,000.00. Bell holds 21,066 awarded Monaqasat tenders with a winner on **3**. The CR number is a direct join to `companies`. Parse `<td>` cells (`detailFields`), never scan forward from a label (§2.2), and note the CR cell can carry TWO numbers separated by ` | `.
- A commented‑out `<td>` once shifted Ashghal's winner columns. Strip HTML comments before parsing tables.
- QatarEnergy serves tenders from an anonymous ASMX JSON endpoint — plain `fetch`, no browser needed.
